apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-service-a
  labels:
    app: worker-service-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-service-a
  template:
    metadata:
      labels:
        app: worker-service-a
    spec:
      containers:
        - name: worker-service-a
          image: seu-registro/worker-service:latest
          imagePullPolicy: Always
          env:
            - name: WORKER_NAME
              value: "WorkerServiceA" # Define o nome do worker aqui
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"